# Module kts-io-geojson

GeoJSON geometry reader and writer for the JTS Topology Suite. Package-level documentation
migrated from the per-package `package-info.java` for use as Dokka `includes` in the
Kotlin Multiplatform build.

# Package org.locationtech.jts.io.geojson

Classes to read and write the GeoJSON geometry format.
